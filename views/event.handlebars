
{{#each events as |event|}}
<div class="cards">
  <h2>
  {{name}}
  </h2>
  <p>{{description}}</p>
  <p>{{date}}</p>
  <p>{{time}}</p>
   <p>{{location}}</p>

  
  </p>
  <p>
    <button class="btn btn-primary">Attend this Event!</button>
  </p>
</div>
{{/each}}