<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{description}}</p>
   <p>{{location}}</p>
  <p>Organized by {{user.name}} on {{format_date date}} </p>
  
  </p>
  <p>
    <button class="btn btn-primary">Attend this Event!</button>
  </p>
</div>